# 🏦 سامانه هوشمند مدیریت تسهیلات آتا (ATA System)

یک راهکار جامع و حرفه‌ای برای مدیریت فرآیندهای بانکی، برآورد تسهیلات و سازماندهی پرونده‌های مشتریان با رابط کاربری مدرن و واکنش‌گرا.

---

## ✨ قابلیت‌های کلیدی

### 📊 داشبورد مدیریتی (Dynamic Dashboard)
- **ویجت‌های قابل شخصی‌سازی**: امکان تغییر چیدمان و نمایش/عدم نمایش بخش‌های مختلف داشبورد.
- **تحلیل بصری**: نمایش نمودارهای رشد منابع و وضعیت پرونده‌ها با استفاده از `Recharts`.
- **آمار لحظه‌ای**: دسترسی سریع به تعداد پرونده‌ها، سرمایه جاری و وضعیت‌های بحرانی.

### 👥 مدیریت مشتریان (Customer CRM)
- **ثبت و ویرایش پیشرفته**: فرم‌های هوشمند با اعتبارسنجی دقیق کد ملی و شماره تماس.
- **گردش کار (Workflow)**: مدیریت وضعیت پرونده (منتظر پرداخت، در انتظار مرات، تکمیل شده و ...).
- **پروفایل چاپی (Print-Ready)**: قابلیت تولید و چاپ مستقیم پروفایل مشتری در ابعاد A4 استاندارد.

### 🧮 محاسبه‌گر تسهیلات (Loan Calculator)
- **محاسبات دقیق**: برآورد اقساط، سود و خالص دریافتی بر اساس دوره‌های زمانی مختلف.
- **تبدیل عدد به حروف**: نمایش خودکار مبالغ به حروف (تومان/ریال).
- **گزارش‌گیری محاسبات**: امکان چاپ خروجی محاسبات برای ارائه به مشتری.

### 🔐 مدیریت امنیت و کاربران
- **کنترل دسترسی (RBAC)**: تفکیک سطوح دسترسی بین «مدیر ارشد» و «کارشناس».
- **امنیت حساب**: قابلیت تایید دو مرحله‌ای (2FA) و مدیریت جلسات کاری.
- **تاریخچه گفتگو**: بایگانی کامل تعاملات و پیام‌های رد و بدل شده با مشتریان.

---

## 🛠 پشته تکنولوژی (Tech Stack)

| بخش | تکنولوژی |
| :--- | :--- |
| **کتابخانه اصلی** | React 18.3 |
| **طراحی UI** | Tailwind CSS |
| **انیمیشن‌ها** | Framer Motion |
| **آیکون‌ها** | Lucide React |
| **نمودارها** | Recharts |
| **مدیریت مسیرها** | React Router DOM v6 |
| **فونت** | Vazirmatn (وزیرمتن) |

---

## 📂 ساختار پروژه

```text
src/
├── components/       # اجزای مشترک (Sidebar, Header, ErrorBoundary)
├── pages/            # صفحات اصلی سامانه
│   ├── Dashboard.tsx      # پیشخوان مدیریتی
│   ├── CustomerList.tsx   # لیست مشتریان
│   ├── CustomerForm.tsx   # فرم ثبت/ویرایش
│   ├── FacilityEstimation.tsx # محاسبه‌گر وام
│   └── ...
├── utils/            # توابع کمکی (فرمت مبالغ، تاریخ شمسی)
├── hooks/            # هوک‌های سفارشی (Window Dimensions)
├── types.ts          # تعاریف تایپ‌های TypeScript
└── constants.ts      # داده‌های اولیه و Mock Data
```

---

## 🚀 راهنمای بیلد و اجرا

این پروژه با استفاده از `Vite` پیکربندی شده است تا بالاترین سرعت و بهینگی را در زمان اجرا داشته باشد.

1. **نصب وابستگی‌ها**:
   ```bash
   npm install
   ```

2. **اجرا در محیط توسعه**:
   ```bash
   npm run dev
   ```

3. **خروجی نهایی (Build)**:
   ```bash
   npm run build
   ```
   *فایل‌های خروجی به صورت بهینه در پوشه `dist` سازماندهی می‌شوند (JS در پوشه `js` و استایل‌ها در پوشه `css`).*

---

## 📝 نکات فنی مهم
- تمام مبالغ در سیستم به صورت خودکار با جداکننده سه رقمی و اعداد فارسی نمایش داده می‌شوند.
- سیستم دارای `ErrorBoundary` سراسری است تا از کرش کردن برنامه در صورت بروز خطاهای غیرمنتظره جلوگیری شود.
- طراحی کاملاً **Mobile-First** بوده و در تمامی دستگاه‌ها (موبایل، تبلت و دسکتاپ) به درستی نمایش داده می‌شود.

---
**توسعه یافته با ❤️ برای مدیریت هوشمند تسهیلات.**
